name: l_enchere
description: "Application d'enchère rapide."
publish_to: 'none' 
version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.0.5
  uuid: ^3.0.7
  intl: ^0.18.0
  cupertino_icons: ^1.0.8
  image_picker: ^1.0.7
  photo_view: ^0.15.0
  shared_preferences: ^2.3.2
  google_sign_in: ^6.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true